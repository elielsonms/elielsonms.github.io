{
  "name": "elielsonms-github-io",
  "private": true,
  "scripts": {
    "build:clean": "rm -rf dist && mkdir -p dist/styles dist/scripts",
    "build:html": "node scripts/generate-html.js",
    "build:pdf": "sh scripts/run-python.sh scripts/generate_pdf.py",
    "build": "npm run build:clean && npm run build:html && npm run build:pdf"
  }
}
